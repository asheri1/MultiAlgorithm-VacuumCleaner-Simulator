#ifndef UTILS_H
#define UTILS_H

#include <string>

// Function to write an error message to a specified file
void writeErrorToFile(const std::string& filename, const std::string& error_message);

#endif // UTILS_H
